<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Threadify</title>
     <script src="https://cdn.tailwindcss.com"></script>
     <style>
    body {
      background: linear-gradient(135deg, #680571cb, #0d055e, #25254c);
    }

    .glass-card {
      background: rgba(97, 2, 2, 0.341);
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
      border: 1px solid rgba(63, 2, 2, 0.181);
    }

    .form-input {
      @apply w-full px-4 py-2 rounded-md bg-white/60 text-gray-800 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-400;
    }

    .form-button {
      @apply w-full py-2 rounded-md font-semibold bg-gradient-to-r from-cyan-500 to-blue-500 hover:from-cyan-600 hover:to-blue-600 transition;
    }
  </style>
</head>
<body class="bg-zinc-900 text-white">
    <div class="min-h-screen flex items-center justify-center px-4">
    <div class="max-w-5xl w-full grid grid-cols-1 md:grid-cols-2 gap-10">
        <!----Create Account Section---->
            <div class="card bg-black/70 backdrop-blur-md p-8 rounded-2xl shadow-lg">
            <h2 class="text-2xl font-semibold mb-6 text-center">Create Account</h2>
            <form action="/register" method="post" class="space-y-4">
                <input class="px-3 py-2 rounded-md bg-zinc-700 placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 bg-transparent border-2 border-zinc-800 block w-full" type="text" placeholder="name" name="name">
                <input class="px-3 py-2 rounded-md bg-zinc-700 placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 bg-transparent border-2 border-zinc-800 block w-full" type="text" placeholder="username" name="username">
                <input class="px-3 py-2 rounded-md bg-zinc-700 placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 bg-transparent border-2 border-zinc-800 block w-full" type="password" placeholder="password" name="password">
                <input class="px-3 py-2 rounded-md bg-zinc-700 placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 bg-transparent border-2 border-zinc-800 block w-full" type="email" placeholder="email" name="email">
                <input class="px-3 py-2 rounded-md bg-zinc-700 placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 bg-transparent border-2 border-zinc-800 block w-full" type="number" placeholder="age" name="age">
                <button class="px-5 py-2  bg-blue-600 hover:bg-blue-700 rounded-md text-white font-semibold" type="Submit">Create Account</button>
            </form>
            </div>

            <!--Login Section-->
            <div class="card bg-black/70 backdrop-blur-md  p-8 rounded-2xl shadow-lg ">
            <h3 class="text-2xl mb-6 font-semibold text-center">Login Your Account</h3>
            <form action="/login" method="post" class="space-y-4">
                <input class="px-3 py-2 rounded-md bg-zinc-700 placeholder-gray-300  focus:outliner-none focus:ring-2 focus:ring-blue-50 bg-transparent border-2 border-zinc-800 block w-full" type="email" placeholder="email" name="email">
                <input class="px-3 py-2 rounded-md bg-zinc-700 placeholder-gray-300  focus:outliner-none focus:ring-2 focus:ring-blue-50 bg-transparent border-2 border-zinc-800 block w-full" type="password" placeholder="password" name="password">
                <button class="px-5 py-2 rounded-md bg-blue-600 hover:bg-blue-700 text-white font-semibold" type="Submit">Login Account</button> 
            </form>
            </div>
        </div>
    </div>
</body>
</html>