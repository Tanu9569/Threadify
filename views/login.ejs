<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | Threadify</title>
     <script src="https://cdn.tailwindcss.com"></script>
     <style>
    body {
      background: linear-gradient(135deg, #141e30, #243b55);
    }

    .glass {
      background: rgba(255, 255, 255, 0.07);
      backdrop-filter: blur(14px);
      -webkit-backdrop-filter: blur(14px);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
  </style>
</head>
<body class="min-h-screen flex items-center justify-center font-sans text-white">
    <div class="glass rounded-xl shadow-lg p-10 w-full max-w-md">
        <h3 class="text-3xl mb-6 font-bold text-center text-cyan-300">🔐 Login to Threadify</h3>

        <form action="/login" method="post" class="space-y-6">
          <div>
            <label class="block text-sm mb-1 text-gray-300">Email</label>
            <input class="w-full px-4 py-2 rounded-md bg-zinc-900 text-white placeholder-gray-400 border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-cyan-500" 
            type="email" 
            placeholder="email" 
            name="email">
          </div>
          <div>  
            <label class="block text-sm mb-1 text-gray-300">Password</label>
            <input 
              class="w-full px-4 py-2 rounded-md bg-zinc-900 text-white placeholder-gray-400 border border-zinc-700 focus:outline-none focus:ring-2 focus:ring-cyan-500"
              type="password"
              placeholder="password" 
              name="password">
          </div> 
          <div>
            <button 
              class="w-full py-2 rounded-md bg-gradient-to-r from-cyan-500 to-indigo-500 hover:from-cyan-600 hover:to-indigo-600 text-white font-semibold shadow-md transition"
              type="submit">
                Login Account
            </button>
          </div>
        </form>
       
         <!-- Footer -->
        <p class="text-center text-sm text-gray-400 mt-6">
            Don’t have an account? 
          <a href="/register" class="text-cyan-400 hover:underline">Register</a>
        </p>
      </div>
</body>
</html>